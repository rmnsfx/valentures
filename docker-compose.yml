networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    depends_on:
      - game
      - frontend
    volumes:
      - "nginx.conf:/etc/nginx/nginx.conf"
    networks:
      - dev

  game:
    build:
      context: ./game
    networks:
      - dev
      
  frontend:
    build:
      context: ./frontend
    networks:
      - dev